version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/raw"
    schema: "db-client/migrations"
    gen:
        go:
            package: "queries"
            out: "sql"
            sql_package: "pgx/v4"

