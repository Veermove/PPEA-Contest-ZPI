// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.24.0
// source: check_email_tracker_exists.sql

package queries

import (
	"context"
)

const checkEmailTrackerExist = `-- name: CheckEmailTrackerExist :one
select exists(
    select assessor_id, submission_id, rating_type, emails_sent
    from emails.sent_for_one_rating
    where
        assessor_id = $1
        and
        submission_id = $2
        and
        rating_type = $3
) as exists
`

type CheckEmailTrackerExistParams struct {
	AssessorID   int32
	SubmissionID int32
	RatingType   ProjectRatingType
}

func (q *Queries) CheckEmailTrackerExist(ctx context.Context, arg CheckEmailTrackerExistParams) (bool, error) {
	row := q.db.QueryRow(ctx, checkEmailTrackerExist, arg.AssessorID, arg.SubmissionID, arg.RatingType)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}
